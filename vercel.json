{
    "framework": "nextjs",
    "buildCommand": "rm -rf .next && npm run build",
    "installCommand": "npm install",
    "functions": {
        "src/proxy.ts": {
            "runtime": "edge"
        }
    }
}